# Issuance lifecycle

REQUESTED -> PREFLIGHT -> AUTHORIZED -> ISSUED -> ACTIVE -> FROZEN/REVOKED/EXPIRED/REDEEMED

Card replacement: REQUESTED -> close existing card -> issue replacement -> relink account -> audit.

Token issuance: REQUESTED -> reserve attestation -> compliance/risk/insurance gates -> mint authorization -> ledger reconciliation -> audit.
